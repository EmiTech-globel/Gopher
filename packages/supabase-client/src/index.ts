// Platform-agnostic Supabase client factory.
// Each app (mobile, admin) injects its own auth storage adapter
// (SecureStore for mobile, cookies for admin) — intentionally
// left unimplemented until we design that interface together.
export {};
